# Segredos e config real (NUNCA versionar — contém a senha do viewer)
config.local.json
config.local.json.bak*

# Saída gerada (arquivos que alimentam os consumidores)
saida/*
!saida/.gitkeep

# WhatsApp (sessao Baileys = credencial; nunca versionar)
scripts/whatsapp/auth/
scripts/whatsapp/node_modules/
scripts/whatsapp/package-lock.json

# Robo de upload (perfil do navegador = sessao logada no claude.ai = credencial)
robo/config_robo.json
robo/perfil_navegador/
robo/robo_upload.log
robo/ultima_falha.png
robo/backups/

# Logs
*.log
bridge_erros.log

# Python
__pycache__/
*.pyc
.venv/
venv/
